{
  "name": "gallery",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "A quiet, white photo book generated as a static site.",
  "scripts": {
    "build": "node src/build.mjs",
    "clean": "node -e \"fs.rmSync('dist',{recursive:true,force:true})\"",
    "preview": "npm run build && node --run serve",
    "serve": "node src/serve.mjs"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "exif-reader": "^2.0.2",
    "sharp": "^0.35.4"
  }
}
